<div>
    {{#if big}}
    <h2 class="mb-5">{{data}}</h2>
    {{else}}
    {{!-- сюда вписуємо зміну яку хочемо використати в заголовку в даному випадку зміна (data) --}}
    <h3 class="mb-3">{{data}}</h3>
    {{/if}}
</div>

<style>
    h2 {
        color: green;
    }

    h3 {
        color: green;
    }
</style>